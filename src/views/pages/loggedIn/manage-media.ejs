<%- include('../../partials/head.ejs') %> 
<%- include('../../partials/nav.ejs') %> 
<% const deleteClass = `delete-${mediaType}-url`; %> 
<div class="manage-page">
  <h2 class="manage-page-title">Manage <%= mediaType === 'video' ? 'YouTube videos' : 'Soundcloud tracks' %></h2>
  <p><a type="button" class="btn btn-primary" href="/config/create-<%= mediaType %>">Add new</a></p>
  <div class="manage-page-panel">
    <div class="manage-page-panel__header">
      <p class="text-primary">Title</p>
      <p class="text-primary">URL</p>
      <p class="text-secondary action-button">Edit?</p>
      <p class="text-danger action-button">Delete?</p>
    </div>
    <% items.forEach(({ title, url }) => { %>
      <div class="manage-page-panel__item">
        <p><%= title %></p>
        <p><a href="<%= url %>" target="_blank"><%= url %></a></p>
        <p class="action-button"><a type="button" class="btn btn-secondary" href="/config/update-<%= mediaType %>?url=<%= url %>">Edit</a></p>
        <p class="action-button"><button type="submit" class="btn btn-danger" onclick="updateFormValue('<%= deleteClass %>', '<%= url %>');">Delete</button></p>
      </div>
    <% }); %> 
  </div>
</div>
<%- include('../../components/forms/delete-item-form.ejs', {
  dataType: mediaType,
  formValueType: 'url',
  sendMessage,
}) %>
<%- include('../../components/modal.ejs', {
  modalTitle: 'Delete',
  modalText: `Are you sure you want to delete this ${mediaType}?`,
  modalTitle: `Delete ${mediaType}`,
  formClass: `delete-${mediaType}-form`,
}) %>
<%- include('../../partials/foot.ejs') %> 